// Nome do pacote
package button;

// Criação da interface botão, a qual irá implementar determinado método 
// para assegurar que as classes que irâo utilizá-la utilizem os métodos disponibilizados.
public interface Button {
	
	// Instancia do método paint(), o qual será implementado nas classes subsequentes.
	void paint();
}
